#define PLUGIN_VERSION "1.2.3"

#include <sourcemod>

public Plugin myinfo = {
    name = "Test Plugin",
    author = "Test",
    description = "Test plugin",
    version = PLUGIN_VERSION,
    url = ""
};
